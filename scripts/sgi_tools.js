const toolDir = "/fw64/sgi2pc/tool";

const toolPaths = {
    adpcmenc: `${toolDir}/ADPCMENC.EXE`,
    midicvt: `${toolDir}/MIDICVT.EXE`,
    midicomp: `${toolDir}/midicomp.exe`,
    sbc: `${toolDir}/SBC.EXE`,
    tabledesign: `${toolDir}/tabledesign.exe`,
    ic: `${toolDir}/ic.exe`,
};

module.exports = toolPaths;
